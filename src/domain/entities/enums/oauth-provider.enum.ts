/**
 * OAuth providers supported by the auth domain.
 * Values match the provider strings stored in the database.
 */
export enum OAuthProvider {
  Google = 'google',
  Facebook = 'facebook',
  Github = 'github',
}
